---

# Note: Try and limit these to packages that are distro-specific, and
# place commonly-named ones below.
- name: Install Amazon-Linux-specific prerequisites
  ansible.builtin.include_tasks: "{{ ansible_distribution | lower }}-{{ ansible_distribution_version }}.yml"
  when: ansible_distribution | lower == 'amazon'

- name: Install Ubuntu/Debian-specific prerequisites
  ansible.builtin.include_tasks: "{{ ansible_distribution | lower }}.yml"
  when: ansible_distribution | lower != 'amazon'

- name: Install common support packages
  ansible.builtin.package:
    name:
      - amazon-ec2-utils
      - fontconfig
      - jq
      - net-tools
      - nvme-cli
      - tar
      - yq

- name: Create product group
  ansible.builtin.group:
    name: "{{ atl_product_user }}"
    gid: "{{ atl_product_user_uid }}"

- name: Create product user
  ansible.builtin.user:
    name: "{{ atl_product_user }}"
    uid: "{{ atl_product_user_uid }}"
    group: "{{ atl_product_user }}"
    comment: "Product runtime user"

- name: Mount ebs volumes
  ansible.builtin.include_tasks: mount_ebs_filesystem.yml
  loop: "{{ atl_ebs_filesystems | dict2items }}"

- name: Test aws cli is installed
  stat:
    path: "/usr/local/bin/aws"
  register: test_aws_installed
    
- name: Install AWS CLI
  ansible.builtin.include_tasks: install_aws_cli.yml  
  when: not test_aws_installed.stat.exists
  
- name: Stop systemd-cleanup deleting the jvm socket file
  ansible.builtin.copy:
    src: java.conf
    dest: "/usr/lib/tmpfiles.d/java.conf"
    owner: root
    group: root
    mode: "644"
  register: systemd_config_changed

- name: Add entries to hosts file for all domains served by the ALB
  ansible.builtin.copy:
    src: add_alb_hosts_local.sh
    dest: "/usr/local/bin/add_alb_hosts_local.sh"
    owner: root
    group: root
    mode: "500"

- name: Run add_alb_hosts_local.sh
  ansible.builtin.copy:
    src: add_alb_hosts_local.service
    dest: "/etc/systemd/system/add_alb_hosts_local.service"
    owner: root
    group: root
    mode: "644"
    daemon_reload: true
    
- name: Run add_alb_hosts_local.sh on 1 minute after boot then every 30 minutes
  ansible.builtin.copy:
    src: add_alb_hosts_local.timer
    dest: "/etc/systemd/system/add_alb_hosts_local.timer"
    owner: root
    group: root
    mode: "644"
    state: started
    enabled: true
    daemon_reload: true
    

#
# !!! WARNING WARNING WARNING !!!
#
# Using entries in /etc/hosts to override DNS can only return a single address for a given hostname
# Do not use this in production!!!!
#
# - name: Lookup DNS for ALB
#   ansible.builtin.set_fact:
#     alb_addresses: "{{ query('community.dns.lookup', atl_alb_endpoint) }}"

# - name: Set state for host overrides
#   ansible.builtin.set_fact:
#     hosts_overrides_state: "{{ atl_enable_hosts_overrides | ternary('present', 'absent') }}"
    
# - name: Add servers to hosts file
#   ansible.builtin.lineinfile:
#     path: /etc/hosts
#     line: "{{ item }} {{ atl_hosts_overrides }}"
#     state: "{{ hosts_overrides_state }}"
#   loop: "{{ alb_addresses }}"

